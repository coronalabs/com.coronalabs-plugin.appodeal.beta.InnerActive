Sources for the plugin `plugin.appodeal.beta.InnerActive`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.InnerActive" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
